# ticktick-cli environment template
# Duplicate this file as .env and fill in values.
#
# Quick start:
# 1. Create an app in TickTick Developer Portal: https://developer.ticktick.com/manage
# 2. Paste your app credentials below
# 3. Run `ticktick auth` (or `ticktick auth --manual`) to get an access token
# 4. Paste the token into TICKTICK_ACCESS_TOKEN

# OAuth app credentials (required)
TICKTICK_CLIENT_ID=
TICKTICK_CLIENT_SECRET=
TICKTICK_REDIRECT_URI=http://127.0.0.1:8080/callback
TICKTICK_ACCESS_TOKEN=

# Account login for V2/session API (required)
TICKTICK_USERNAME=
TICKTICK_PASSWORD=
